|——————————SR配网
|————————代码整体逻辑：麦克风采集数据->AFE处理(唤醒词检测+VAD人声检测)->回调通知主程序->
                                            ->唤醒后人声数据存入环形缓冲区->取数据播放

|————————主要代码功能：
|——————audio_sr.c:
|——audio_sr_feed_task(void* arg):从ES8311读取麦克风原始PCM，持续喂给AFE库
{
先拿到arg参数；
根据实例中我们之前配置的采样率以及其内部规定的一个数据时长计算每次处理数据大小的一个计算值；
获取我们在afe_data中配置的通道数
根据位深以及数据时长采样率计算每次处理数据的大小:单通道处理数*通道数*实际字节大小
根据算出的数据大小开辟出一个数据缓存区
启动喂养：不停的给他扔音频数据，从麦克风里面不断地读取数据，有数据了就喂给afe_data去处理；
}
|——audio_sr_fetch_task(void* arg):从AFE库取处理结果，判断唤醒词/VAD，触发回调，并且保存数据
{
先拿到arg参数；
持续的取出我们之前通过audio_sr_feed_task存进去的数据;
取出数据整体result；
截取数据整体内的音频数据processed_audio；
截取数据整体中的人声检测结果vad_state;
截取数据整体中的唤醒词检测结果is_wakeup；
通过获取到的wakeuo_state状态去判断是否被唤醒，如果被唤醒就把is_wakeup置一，同时调用回调函数wakeup_cb处理；
在被唤醒的状态下，我们再判断vad(语音活动状态)状态，如果语言活动状态和上次不一样，我们就调用回调函数vad_cb处理；
同时更新vad状态；
同时在被唤醒的时候，如果vad是检测到人声的话，我们就把音频数据通过函数xRingbufferSend放到环形缓冲区内；
}
|——audio_sr_creat(void):动态创建结构体变量并返回该结构体变量；
|——audio_sr_register_vad_cb(audio_sr_t *audio_sr,void(*vad_cb)(vad_state_t vad_state)):
{
创建检测人声活动的注册回调函数；
audio_sr_t *audio_sr：结构体变量，将注册的回调函数地址放入该结构体；
void(*vad_cb)(vad_state_t vad_state)：将要注册的回调函数；
}
|——audio_sr_register_wakeup_cb(audio_sr_t *audio_sr,void(*wakeup_cb)(void)):
{
创建被唤醒后的注册回调函数；
audio_sr_t *audio_sr：结构体变量，将注册的回调函数地址放入该结构体；
void(*wakeup_cb)(void)：将要注册的回调函数；
}
|——audio_sr_register_ringbuf(audio_sr_t *audio_sr, RingbufHandle_t ringbuf):
{
给SR注册接收已经被处理好的音频数据缓冲区；
audio_sr_t *audio_sr：结构体变量，将注册的环形缓冲区放入该结构体；
RingbufHandle_t ringbuf：将要注册的环形缓冲区；
}
|——audio_sr_start(audio_sr_t* audio_sr)：做一些SR的起始工作和创建AFE实例
{
初始化AFE配置：加载乐鑫的语音模型库并命名为model；
初始化AFE配置结构体：单麦，模型句柄，类型：语音识别，模式：高性能；
配置AFE的音频预处理器开关：关闭AEC(回声消除)，关闭SE(声源增强),关闭NS(降噪),关闭AGC(自动增益控制)；
配置AFE的VAD配置：打开VAD，VAD模式：最灵敏
配置AFE的唤醒词检测：打开WakeNet唤醒词检测，唤醒词检测模式：95%识别率(平衡模式)；
配置AFE的通用音频参数：总通道数：1，麦克风数：1，采样率：16000，AFE模式：高性能，AFE类型：语音识别，内存分配；
创建AFE句柄和实例：将之前配置的所有配置结合成这一个句柄内，再创建一个实例保存AFE运行时的所有状态；
创建feed和fetch任务；
}
|——————main.c:
|——app_main_wakenet_cb(void):创建唤醒检测回调函数
{
创建该回调函数，当底层接收到相应状态时执行该回调函数；
}
|——app_main_vad_cb(vad_state_t vad_state):创建人声活动状态回调函数
{
创建该回调函数，当底层接收到相应状态时执行该回调函数；
}
|——app_main():进行初始化，注册回调以及从环形缓冲区取数据播放
{
初始化es8311芯片；
创建一个SR实例；
创建一个audio的64*1024字节的环形缓冲区用来存放被处理好的音频数据；
将创建的环形缓冲区地址通过注册回调函数送到底层结构体内保存；
注册人生检测和唤醒的回调函数，并将我们之前创建的回调函数app_main_vad_cb和app_main_wakenet_cb地址传到底层；
启动SR；
创建变量存放读取出来的音频数据以及数据长度；
while(1)循环不断的轮询读取数据后将数据交给扬声器后释放资源，在做一个10ms的堵塞延时；
}
ps：回调函数执行逻辑：主函数定义回调函数，并注册回调函数(将自己的函数地址给到其他模块)，其他模块用结构体
或变量存储下函数地址，其他模块定义注册函数，函数地址就是通过该函数传递，在模块中调用该函数地址，即当触发
相应逻辑后，该模块就会直接调用主函数定义的回调函数(有参也会传参)；
    作用：其他模块并不知道某个逻辑被触发后用户需要干什么，就放一个地址入口在外部(主函数内)，用户自己定义
函数逻辑，两边地址完成同步即可在相应逻辑被触发后执行；
    步骤：创建注册回调函数->创建回调函数->注册回调函数将函数地址通过送到底层->底层触发相应动作执行回调函数；
